# 管理後台使用方式

網址：https://tarokoit.github.io/talkroom/admin.html

## 第一次登入

1. 使用公司指定的三個管理員信箱之一，輸入自行設定的管理員密碼（至少 12 字）。房間密碼與管理員密碼不同。
2. 點「首次建立帳號」，到自己的信箱完成驗證，再回到後台登入。
3. 帳號必須在伺服器管理員名單內且完成信箱驗證，不能只靠知道信箱名稱取得權限。

專案寄信與驗證導回網址需由 Supabase 管理者完成設定：

- Authentication → URL Configuration：Site URL 設為 `https://tarokoit.github.io/talkroom/`，Redirect URLs 加入 `https://tarokoit.github.io/talkroom/admin.html`。
- Authentication → Emails → SMTP Settings：若尚未設定自訂 SMTP，請使用公司的寄信服務設定主機、Port、帳號、密碼與寄件地址。SMTP 密碼只填在 Supabase 設定，勿放進網站或 GitHub。
- Supabase 預設寄信服務通常只寄給專案組織的成員，而且目前限制每小時 2 封；不能直接假設三個公司信箱都能收到。不需要為了收信而把員工加入 Supabase 專案管理團隊。
- 若驗證後導到 localhost，先完成上述 URL 設定，再回到正式後台確認是否可以登入；不要重複建立同一帳號。

目前程式已提供註冊／登入流程與伺服器授權；三個真實信箱的投遞及首次登入需要各信箱持有人完成驗證，尚未由自動測試確認。

官方說明：[SMTP](https://supabase.com/docs/guides/auth/auth-smtp)、[Redirect URLs](https://supabase.com/docs/guides/auth/redirect-urls)。

## 房間管理

- 直接改房間名稱，再儲存。房間代碼不會變，因此歷史記錄仍屬於同一房間。
- 密碼欄空白表示不變。新密碼至少 12 字、最多 64 UTF-8 bytes；建議使用英文與數字組合。
- 更改密碼後，該房間全部裝置須重新登入。廣播房包含控制台及工作站。
- 「讓全房重新登入」只撤銷目前登入；要防止持有舊密碼的人回來，必須同時更換密碼。

## 搜尋與匯出

1. 選文字對話或登入／登出。
2. 指定房間、起訖日期、關鍵字；可留空。
3. 通常選 AND，例如「房務部 AND 9/1–9/12 AND 毛巾」。OR 會列出符合任一條件的資料，可能包含其他房間或日期。
4. 多個關鍵字用逗號分隔，再選關鍵字之間為 AND 或 OR。
5. 點搜尋；每頁 100 筆。點匯出會依目前填寫的條件產生 CSV，最多 10,000 筆，超過時縮小範圍。日期包含結束日整天，使用台灣時間。

登入登出事件從 v2.1 開始記錄。斷網／關閉程式可能無法送出登出要求，這種情況顯示逾時離線推估，不能視為精確的出勤時間。文字搜尋涵蓋目前六房間版本的記錄，不會混入更早 v1 的舊表。

## 清除文字記錄

1. 在獨立的清除區選「一個房間」與開始／結束日期。
2. 點「預覽清除筆數」。
3. 檢查房間、日期與筆數；按「確認永久清除」後還會再確認一次。

清除範圍永遠是「指定房間 AND 指定日期區間」。搜尋區的 OR 與關鍵字不影響此功能。預覽有效 5 分鐘；修改條件必須重新預覽。只刪預覽時列入的文字記錄，不刪登入登出紀錄或預覽後的新訊息。永久清除無法在後台復原，需要保留時先匯出。

## 通訊畫面

- Enter 換行，Ctrl+Enter 或 Command+Enter 傳送，也可按傳送鈕。
- 成員名字綠色：收聽開啟；紅色：收聽關閉；黃色底：偵測到音訊，環境噪音也可能觸發。
- 音量偵測不會錄音，且需要語音連線與瀏覽器音訊正常運作。鎖屏後的可靠性仍待 Android App 實機驗證。
- 普通房間最多 15 個在線裝置，第 16 個由伺服器拒絕加入。廣播房是 1 台控制台加 15 台收聽工作站。
- 60 人不是目前的全系統硬上限，也尚未完成 60 台實機壓力測試。多位同時開麥、網路上傳速度及 Wi-Fi 都會影響表現。
- 近距離回授優先用耳機，或同一空間只留一台喇叭。降低喇叭音量並避免多台同時開麥；瀏覽器回音消除不能保證抑制多設備形成的回授。

## Android

已確定走公司私下安裝的簽署 APK，不上 Google Play，Windows 瀏覽器仍保留並共用相同房間與資料庫。APK 尚未製作；正式開發前先驗證原生背景音訊與網頁互通、跨網路 TURN、鎖屏後新廣播、Wi-Fi/行動網路切換及更新簽章流程。詳見 ANDROID-PLAN.md。
